# Streamlit Secrets 設定ファイル（サンプル）
# このファイルをコピーして secrets.toml を作成してください
# ⚠️ secrets.toml は Git管理しないこと（.gitignoreに追加）

# Cartesia API設定
[cartesia]
api_key = "your-cartesia-api-key-here"  # Cartesia APIキー
voice_id = "your-voice-id-here"         # 声クローンID

# D-ID API設定
[did]
api_key = "your-did-api-key-here"       # D-ID APIキー

# Cloudinary設定
[cloudinary]
cloud_name = "your-cloud-name"          # Cloudinaryクラウド名
api_key = "your-cloudinary-api-key"     # Cloudinary APIキー
api_secret = "your-cloudinary-api-secret" # Cloudinary APIシークレット

# アプリケーション設定（オプション）
[app]
password = "your-secure-password"       # アプリケーションパスワード（オプション）

# 使用方法:
# 1. このファイルを .streamlit/secrets.toml にコピー
# 2. 各APIキーを実際の値に置き換え
# 3. .gitignore に secrets.toml を追加

# Streamlit Cloudでの設定:
# 1. Streamlit Cloud Dashboard → App settings → Secrets
# 2. 上記の内容をコピー＆ペースト
# 3. 各値を実際のAPIキーに置き換え
