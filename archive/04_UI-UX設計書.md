# UI/UX設計書

**プロジェクト名**: ブログ記事→YouTube動画自動生成システム
**バージョン**: 1.0.0
**作成日**: 2025年11月15日

---

## 📋 目次

1. [設計方針](#設計方針)
2. [画面設計](#画面設計)
3. [コンポーネント設計](#コンポーネント設計)
4. [状態管理](#状態管理)
5. [ユーザーフロー](#ユーザーフロー)
6. [エラー処理とフィードバック](#エラー処理とフィードバック)

---

## 設計方針

### UI/UX原則

```yaml
シンプルさ:
  - 1画面完結（スクロールのみ）
  - 直感的な操作
  - 最小限の入力項目

非エンジニア向け:
  - 専門用語を避ける
  - ヘルプテキスト豊富
  - エラーメッセージは日本語で明確に

視覚的フィードバック:
  - リアルタイム進捗表示
  - ステップ表示
  - プレビュー機能
```

### Streamlit選定理由

```
✅ Pythonのみで実装可能
✅ 高速プロトタイピング
✅ 自動リロード
✅ Community Cloud無料デプロイ
✅ モバイル対応
```

---

## 画面設計

### メイン画面（全体レイアウト）

```
┌────────────────────────────────────────────────────┐
│  📹 ブログ → YouTube 動画生成                       │
│────────────────────────────────────────────────────│
│                                                    │
│  [サイドバー]          [メインエリア]              │
│  ┌─────────┐         ┌──────────────────────────┐ │
│  │設定      │         │ 入力フォーム             │ │
│  │         │         │                          │ │
│  │- API設定│         │ ▶️ [動画生成開始]       │ │
│  │- 詳細   │         │                          │ │
│  │         │         │ 進捗表示                 │ │
│  │         │         │                          │ │
│  │         │         │ プレビュー               │ │
│  └─────────┘         └──────────────────────────┘ │
│                                                    │
└────────────────────────────────────────────────────┘
```

### 詳細ワイヤーフレーム

```
┌──────────────────────────────────────────────────────┐
│  📹 ブログ → YouTube 動画生成                         │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ## 📝 入力                                           │
│  ┌───────────────────────────────────────────────┐  │
│  │ ブログ記事URL                                  │  │
│  │ [https://blog.example.com/post1            ]  │  │
│  │ 💡 記事URLを入力してください                   │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ## 🎭 アバター選択                                  │
│  ┌────┐ ┌────┐ ┌────┐                             │
│  │ ◉  │ │    │ │    │                             │
│  │[1] │ │[2] │ │[3] │                             │
│  └────┘ └────┘ └────┘                             │
│  スーツ  カジュアル  プレゼン                       │
│                                                      │
│  ## ⚙️ 設定（オプション）                            │
│  ┌─────────────────────────┐                       │
│  │ 声のトーン                                      │
│  │ [標準 ────●──── 早口]                          │
│  │                                                 │
│  │ 動画の長さ                                      │
│  │ ( ) 60秒 (Shorts)  (•) 5分                    │
│  └─────────────────────────┘                       │
│                                                      │
│  ┌──────────────────────┐                          │
│  │  ▶️  動画生成開始     │                          │
│  └──────────────────────┘                          │
│                                                      │
├──────────────────────────────────────────────────────┤
│  ## 📊 進捗状況                                       │
│  ┌───────────────────────────────────────────────┐  │
│  │ ✅ スクリプト生成完了 (5秒)                    │  │
│  │ ━━━━━━━━━━━━━━━━━━━━━                          │  │
│  │                                                 │  │
│  │ "今日は〇〇について解説します..."              │  │
│  │ [📝 編集] [✓ 確定]                             │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ┌───────────────────────────────────────────────┐  │
│  │ ⏳ 音声生成中... 45%                           │  │
│  │ ━━━━━━━━━●──────────────                       │  │
│  │ 予想残り時間: 30秒                              │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ┌───────────────────────────────────────────────┐  │
│  │ ⬜ 動画生成待機中                               │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ┌───────────────────────────────────────────────┐  │
│  │ ⬜ YouTube投稿                                  │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
├──────────────────────────────────────────────────────┤
│  ## 👁️ プレビュー                                    │
│  ┌───────────────────────────────────────────────┐  │
│  │                                                 │  │
│  │         [動画プレビュー]                        │  │
│  │                                                 │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ┌──────────────┐  ┌──────────────┐              │
│  │ 📥 ダウンロード│  │ 📺 YouTube投稿│              │
│  └──────────────┘  └──────────────┘              │
│                                                      │
└──────────────────────────────────────────────────────┘
```

---

## コンポーネント設計

### 1. ヘッダーコンポーネント

```python
def render_header():
    """ヘッダー表示"""
    st.set_page_config(
        page_title="ブログ→YouTube動画生成",
        page_icon="📹",
        layout="wide"
    )

    st.title("📹 ブログ → YouTube 動画生成")
    st.markdown("---")
```

### 2. 入力フォームコンポーネント

```python
def render_input_form():
    """入力フォーム"""
    st.header("📝 入力")

    # ブログURL
    blog_url = st.text_input(
        "ブログ記事URL",
        placeholder="https://blog.example.com/post1",
        help="変換したいブログ記事のURLを入力してください"
    )

    # アバター選択
    st.subheader("🎭 アバター選択")
    col1, col2, col3 = st.columns(3)

    with col1:
        if st.button("スーツ", use_container_width=True):
            st.session_state.avatar = "suit"
    with col2:
        if st.button("カジュアル", use_container_width=True):
            st.session_state.avatar = "casual"
    with col3:
        if st.button("プレゼン", use_container_width=True):
            st.session_state.avatar = "presentation"

    # 設定（オプション）
    with st.expander("⚙️ 詳細設定（オプション）"):
        voice_speed = st.slider(
            "声のトーン",
            min_value=0.5,
            max_value=2.0,
            value=1.0,
            step=0.1,
            help="1.0が標準速度です"
        )

        video_length = st.radio(
            "動画の長さ",
            ["60秒 (Shorts)", "5分"],
            index=0
        )

    return {
        "blog_url": blog_url,
        "avatar": st.session_state.get("avatar", "suit"),
        "voice_speed": voice_speed,
        "video_length": video_length
    }
```

### 3. 進捗表示コンポーネント

```python
def render_progress_step(
    step_name: str,
    status: str,  # "pending", "in_progress", "completed", "error"
    message: Optional[str] = None,
    progress: Optional[float] = None
):
    """
    進捗ステップ表示

    Args:
        step_name: ステップ名
        status: ステータス
        message: メッセージ
        progress: 進捗率（0.0-1.0）
    """
    if status == "completed":
        st.success(f"✅ {step_name}完了")
        if message:
            st.info(message)

    elif status == "in_progress":
        st.info(f"⏳ {step_name}中...")
        if progress is not None:
            st.progress(progress)
            remaining = int((1 - progress) * 60)  # 仮の計算
            st.caption(f"予想残り時間: {remaining}秒")
        if message:
            st.caption(message)

    elif status == "error":
        st.error(f"❌ {step_name}失敗")
        if message:
            st.error(message)

    else:  # pending
        st.markdown(f"⬜ {step_name}待機中")
```

### 4. プレビューコンポーネント

```python
def render_preview(video_url: Optional[str], audio_url: Optional[str]):
    """プレビュー表示"""
    st.header("👁️ プレビュー")

    if audio_url and not video_url:
        st.subheader("🎙️ 音声プレビュー")
        st.audio(audio_url)

    if video_url:
        st.subheader("🎬 動画プレビュー")
        st.video(video_url)

        col1, col2 = st.columns(2)
        with col1:
            if st.button("📥 ダウンロード", use_container_width=True):
                # ダウンロード処理
                pass
        with col2:
            if st.button("📺 YouTube投稿", use_container_width=True):
                # YouTube投稿処理
                pass
```

### 5. エラー表示コンポーネント

```python
def render_error(error: Exception, context: str):
    """エラー表示"""
    st.error(f"""
    ### ⚠️ エラーが発生しました

    **処理**: {context}

    **エラー**: {str(error)}

    **対処方法**:
    1. 入力内容を確認してください
    2. しばらく待ってから再試行してください
    3. 問題が続く場合は管理者に連絡してください

    [トラブルシューティングガイド](/docs/troubleshooting.md)
    """)
```

---

## 状態管理

### セッション状態

```python
# セッション状態の初期化
if "step" not in st.session_state:
    st.session_state.step = "input"  # input, generating, completed

if "script" not in st.session_state:
    st.session_state.script = None

if "audio_url" not in st.session_state:
    st.session_state.audio_url = None

if "video_url" not in st.session_state:
    st.session_state.video_url = None

if "avatar" not in st.session_state:
    st.session_state.avatar = "suit"
```

### 状態遷移

```python
def transition_state(new_state: str):
    """状態遷移"""
    st.session_state.step = new_state
    st.rerun()

# 使用例
if st.button("動画生成開始"):
    transition_state("generating")
```

---

## ユーザーフロー

### フロー図

```
[1] 入力画面
    ↓
    - ブログURL入力
    - アバター選択
    - 設定（オプション）
    ↓
    [動画生成開始] ボタン
    ↓
[2] スクリプト生成
    ↓
    - 進捗表示
    - 完了後、スクリプト表示
    ↓
    [編集可能テキストエリア]
    ↓
    [✓ 確定] ボタン
    ↓
[3] 音声生成
    ↓
    - 進捗バー（%）
    - 完了後、音声プレビュー
    ↓
[4] 動画生成
    ↓
    - ポーリング（5秒間隔）
    - 完了後、動画プレビュー
    ↓
[5] プレビュー・確認
    ↓
    - 動画再生
    ↓
    [📺 YouTube投稿] or [📥 ダウンロード]
    ↓
[6] 完了
    ↓
    - YouTube URL表示
    - [新しい動画を作成] ボタン
```

### シーケンス図（実装レベル）

```python
# app.py メインフロー
def main():
    render_header()

    # 状態に応じて表示を切り替え
    if st.session_state.step == "input":
        render_input_screen()

    elif st.session_state.step == "generating":
        render_generating_screen()

    elif st.session_state.step == "completed":
        render_completed_screen()


def render_input_screen():
    """入力画面"""
    inputs = render_input_form()

    if st.button("▶️ 動画生成開始", use_container_width=True):
        # バリデーション
        if not inputs["blog_url"]:
            st.error("ブログURLを入力してください")
            return

        # 状態遷移
        st.session_state.inputs = inputs
        transition_state("generating")


def render_generating_screen():
    """生成中画面"""
    inputs = st.session_state.inputs

    with st.status("処理中...", expanded=True) as status:
        # ステップ1: スクリプト生成
        render_progress_step("スクリプト生成", "in_progress")
        script, err = generate_script(inputs["blog_url"])

        if err:
            render_error(err, "スクリプト生成")
            return

        st.session_state.script = script
        render_progress_step("スクリプト生成", "completed")

        # スクリプト編集
        edited_script = st.text_area("スクリプト", script, height=200)
        if not st.button("✓ 確定"):
            st.stop()

        # ステップ2: 音声生成
        render_progress_step("音声生成", "in_progress", progress=0.5)
        audio_url, err = generate_audio(edited_script)

        if err:
            render_error(err, "音声生成")
            return

        st.session_state.audio_url = audio_url
        render_progress_step("音声生成", "completed")

        # ステップ3: 動画生成
        render_progress_step("動画生成", "in_progress")
        video_url, err = generate_video(audio_url, inputs["avatar"])

        if err:
            render_error(err, "動画生成")
            return

        st.session_state.video_url = video_url
        render_progress_step("動画生成", "completed")

        status.update(label="完了！", state="complete", expanded=False)

    # 完了後、状態遷移
    transition_state("completed")


def render_completed_screen():
    """完了画面"""
    st.success("🎉 動画生成が完了しました！")

    # プレビュー
    render_preview(
        st.session_state.video_url,
        st.session_state.audio_url
    )

    # アクション
    col1, col2 = st.columns(2)
    with col1:
        if st.button("📺 YouTube投稿", use_container_width=True):
            # YouTube投稿処理
            pass
    with col2:
        if st.button("🔄 新しい動画を作成", use_container_width=True):
            # 状態リセット
            for key in ["script", "audio_url", "video_url", "inputs"]:
                if key in st.session_state:
                    del st.session_state[key]
            transition_state("input")
```

---

## エラー処理とフィードバック

### エラーレベル

```python
# 1. 入力エラー（ユーザー修正可能）
if not blog_url:
    st.warning("⚠️ ブログURLを入力してください")

# 2. 一時的エラー（リトライ可能）
if err:
    st.error("⚠️ API接続エラー。リトライしています...")

# 3. 致命的エラー（運用者対応必要）
if err:
    st.error("""
    ### 🚨 システムエラー

    処理を完了できませんでした。
    管理者に連絡してください。

    エラーID: {error_id}
    """)
```

### 成功フィードバック

```python
# 処理完了
st.success("✅ スクリプト生成が完了しました")

# バルーン演出（完全完了時）
if all_done:
    st.balloons()
    st.success("🎉 動画生成が完了しました！")
```

### ローディング

```python
# スピナー（短時間処理）
with st.spinner("処理中..."):
    result = do_something()

# プログレスバー（長時間処理）
progress_bar = st.progress(0)
for i in range(100):
    progress_bar.progress(i + 1)
    time.sleep(0.1)

# ステータス（複数ステップ）
with st.status("動画生成中...", expanded=True) as status:
    st.write("ステップ1完了")
    st.write("ステップ2実行中...")
    status.update(label="完了！", state="complete")
```

---

**最終更新**: 2025年11月15日
**次回レビュー**: UI実装時
